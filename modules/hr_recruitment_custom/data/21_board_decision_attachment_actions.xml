<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="action_decision_instance_open_generated_pdf" model="ir.actions.server">
            <field name="name">فتح المستند المولد</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_generated_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد المستند المولد مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=0',
        'target': 'new',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_download_generated_pdf" model="ir.actions.server">
            <field name="name">تنزيل المستند المولد</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_generated_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد المستند المولد مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=1',
        'target': 'self',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_open_signed_pdf" model="ir.actions.server">
            <field name="name">فتح المستند الموقع</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_signed_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد المستند الموقع مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=0',
        'target': 'new',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_download_signed_pdf" model="ir.actions.server">
            <field name="name">تنزيل المستند الموقع</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_signed_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد المستند الموقع مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=1',
        'target': 'self',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_open_certificate" model="ir.actions.server">
            <field name="name">فتح شهادة التوقيع</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_certificate_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد شهادة التوقيع مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=0',
        'target': 'new',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_download_certificate" model="ir.actions.server">
            <field name="name">تنزيل شهادة التوقيع</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_certificate_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد شهادة التوقيع مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=1',
        'target': 'self',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_open_stamped_copy" model="ir.actions.server">
            <field name="name">فتح النسخة المختومة</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_stamped_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد النسخة المختومة مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=0',
        'target': 'new',
    }
            ]]></field>
        </record>


        <record id="action_decision_instance_download_stamped_copy" model="ir.actions.server">
            <field name="name">تنزيل النسخة المختومة</field>
            <field name="model_id" ref="grc_backbone.model_decision_instance"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
action = False
record = records.sudo()[:1]
attachment = False

if record:
    attachment = record.x_hr_stamped_attachment_id

if not record:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد قرار', 'message': 'افتح القرار أولاً.', 'type': 'warning', 'sticky': False},
    }
elif not attachment:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {'title': 'لا يوجد ملف', 'message': 'لا يوجد النسخة المختومة مرتبط بهذا القرار.', 'type': 'warning', 'sticky': False},
    }
else:
    action = {
        'type': 'ir.actions.act_url',
        'url': '/web/content/' + str(attachment.id) + '?download=1',
        'target': 'self',
    }
            ]]></field>
        </record>

    </data>
</odoo>