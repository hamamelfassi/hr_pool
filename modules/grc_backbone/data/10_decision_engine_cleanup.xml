<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Pass 7A-6I / 7A-6J SaaS-boundary lock cleanup.

        This file only removes temporary diagnostic/cleanup server actions.
        It does not delete ir.model, ir.model.fields, or ir.model.fields.selection.

        Remaining retired schema metadata, if visible in Odoo technical metadata,
        is a dormant Odoo Online SaaS remnant and must be ignored.
    -->

    <delete model="ir.actions.server" search="[('name', 'in', [
        'تدقيق النماذج القديمة',
        'فك ربط بيانات النماذج القديمة',
        'حذف النماذج القديمة نهائياً',
        'تشخيص بيانات أعمدة النماذج القديمة'
    ])]"/>
</odoo>
